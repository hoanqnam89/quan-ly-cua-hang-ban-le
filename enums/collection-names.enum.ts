export enum ECollectionNames {
  ACCOUNT = `Account`, 
  GOOD_RECEIPT = `Good Receipt`, 
  PRODUCT = `Product`, 
  SUPPLIER = `Supplier`, 
  WAREHOUSE_RECEIPT = `Warehouse Receipt`,
  USER = `User`, 
}
